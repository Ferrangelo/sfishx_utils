### Cosette

A small package containg scripts to plot the fisher matrices produced by SFX_CLASS.

#### Installation

Enter the directory `sfishx_utils` and run `python3 -m pip install --upgrade build` and then `python -m pip install -e .`
